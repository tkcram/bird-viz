import {useEffect, useState} from 'react'
import { useParams } from "react-router-dom";

const Error = (props) => {
	return (
		<div>
			oh no! that class doesn't seem to exist!
		</div>
	)
}

export default Error;
